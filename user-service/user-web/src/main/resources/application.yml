spring:
  datasource:
    url: jdbc:postgresql://postgresdb:5432/unbound
    username: postgres
    password: project123
    driver-class-name: org.postgresql.Driver
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQL9Dialect
    show-sql: true
  application:
    name: user-service
  profiles:
    active: dev

server:
  port: 8100

eureka:
  client:
    register-with-eureka: true
    fetch-registry: false
    service-url:
      # defaultZone: http://gatewayservice:8000/eureka
      # defaultZone: http://localhost:8000/eureka
      # defaultZone: http://iplocal:8000/eureka
      defaultZone: http://apimanager:8000/eureka
  instance:
    leaseRenewalIntervalInSeconds: 5   
    preferIpAddress: true
